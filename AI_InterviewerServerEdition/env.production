# 生产环境配置
export NODE_ENV=production

# 服务器配置
export PORT=5000
export HOST=0.0.0.0

# 数据库配置 - 使用生产环境数据库
export MONGO_URI=mongodb://localhost:27017/ai-interview-prod

# 后端API地址（用于LLM服务调用）
export BACKEND_URL=http://localhost:5000/api

# 前端API地址（使用相对路径，自动适配域名）
export VUE_APP_API_URL=/api 